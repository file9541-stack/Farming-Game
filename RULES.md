# DEV-001 — กฎรวมทั้งหมดของโปรเจกต์ (Cheat Sheet)

> **อ่านก่อนสั่งทุกครั้ง** — กฎเหล่านี้บังคับถาวร AI ต้องปฏิบัติตามทุกข้อทุกการอัปเดต
>
> **DEV-001** = ชุดกฎรวมเดียว ประกอบด้วย กฎ A–B (จาก CLAUDE.md) + DEV RULES ข้อ 1–18
> *(คงเลขข้อ 1–18 เดิมไว้ เพราะโค้ดในเกมอ้างอิง "ข้อ 16/17/18" อยู่)*

---

## DEV-001 · กฎ A–B (Project Rules — จาก CLAUDE.md)

| # | กฎ | ตัวอย่างคำสั่ง |
|---|---|---|
| A | **ฝังรูปเป็น base64 เสมอ** — ทุกรูปที่ใช้ในเกมต้องฝัง data URI ลงในไฟล์ HTML โดยตรง ไม่อ้างไฟล์แยก | "เอาภาพ xxx ไปใส่ในเกม" |
| B | **ส่งไฟล์เดียวจบทันทีหลังอัปเดต** — build FarmGame-standalone.html แล้ว SendUserFile ให้โหลดทดสอบได้เลย | (อัตโนมัติทุกครั้ง) |

---

## DEV-001 · ข้อ 1–7 (การแก้โค้ดพื้นฐาน)

| # | กฎ | ตัวอย่างคำสั่ง |
|---|---|---|
| 1 | **แก้เฉพาะส่วนที่สั่ง** — ห้ามแตะส่วนอื่นที่ไม่ได้รับคำสั่ง | "เปลี่ยนสีปุ่มนี้อย่างเดียว" |
| 2 | **ห้ามลบ/เขียนใหม่** — ส่วนที่ไม่ได้สั่งให้ลบ ห้ามลบ ห้ามเขียนใหม่ทับ | — |
| 3 | **ย้าย = คัดลอกแล้วย้าย** — ถ้าต้องปรับโครงสร้าง ใช้วิธีคัดลอกของเดิมไปแทน ไม่ลบแล้วเขียนใหม่ | "ย้ายบล็อกนี้ไปไว้ด้านบน" |
| 4 | **อ่านก่อนแก้เสมอ** — ก่อนแก้ทุกครั้งต้อง view ไฟล์ก่อน แก้เฉพาะ block ที่เกี่ยวข้องด้วย str_replace | — |
| 5 | **คำพูด NPC สอดคล้อง 3 มิติ** — สินค้า / เพศ (ผม-ครับ / ดิฉัน-ค่ะ) / ระดับราคา (สูง=อยากได้ / ต่ำ=อ้างข้อบกพร่อง) | "ให้ NPC หญิงต่อรองราคา" |
| 6 | **แจ้งสรุปทุกครั้งที่อัปเดต** — ส่งข้อความบอกว่าเปลี่ยนอะไรไปบ้างก่อนเสมอ | (อัตโนมัติทุกครั้ง) |
| 7 | **คิดเองได้เฉพาะเมื่อสั่ง** — ถ้าไม่ได้สั่งให้คิด ห้ามเพิ่มฟีเจอร์เองโดยพลการ | "คิดออกแบบให้เลย" |

---

## DEV-001 · ข้อ 8–10 (โครงสร้าง Section)

| # | กฎ | ตัวอย่างคำสั่ง |
|---|---|---|
| 8 | **เพิ่มโค้ดในส่วนที่กำหนดเท่านั้น** | |
| | · ข้อมูล (พืช/สัตว์/ปลา) → `[data]` | "เพิ่มพืชใหม่" |
| | · State / Helper → `[core]` | "เพิ่ม helper ใหม่" |
| | · Logic ฟาร์ม/โรงงาน/ตลาด → `[farm]` | "แก้ logic ขาย" |
| | · Overlay/Modal → `[overlays]` | "เพิ่ม popup ใหม่" |
| | · Event binding → `[init]` | "ผูก event ปุ่มใหม่" |
| | · CSS ทั่วไป/animation → `[base]` | "แก้สี animation" |
| | · CSS UI หลัก (topbar/farm) → `[game-ui]` | "แก้ layout topbar" |
| | · CSS overlay/modal → `[overlays css]` | "แก้สี popup" |
| 9 | **ห้ามเขียนโค้ดปนกันข้าม Section** — เช่น ห้ามใส่ data ใน [core] | — |
| 10 | **Event ใหม่ต้องอยู่ใน [init] เท่านั้น** — ห้ามใช้ `onclick=` ใน HTML, ห้ามใช้ innerHTML เด็ดขาด; ถ้า element จาก JS → ใช้ event delegation บน container | "เพิ่มปุ่มแล้วผูก event" |

---

## DEV-001 · ข้อ 11–15 (กระบวนการทำงาน)

| # | กฎ | ตัวอย่างคำสั่ง |
|---|---|---|
| 11 | **ยืนยันก่อนทำเสมอ** — อธิบายก่อนว่าจะทำออกมาประมาณไหน แล้วถามว่า "ต้องการประมาณนี้ใช่ไหม?" รอยืนยันก่อนลงมือ | "เปลี่ยนหน้าหลักให้ใหม่" |
| 12 | **ค้นหาแบบเจาะจง (grep)** — เวลาสั่งแก้ระบบใด ให้ grep หาโค้ดหลักของระบบนั้นก่อน ไม่ดึงโค้ดทั้งหมดมาดู | "แก้ระบบซื้อที่ดิน" |
| 13 | **บันทึก UPDATE_HISTORY ทุกครั้ง** — หลังอัปเดตเสร็จ ต้องเพิ่มรายการใน `UPDATE_HISTORY` เวอร์ชัน +0.01 พร้อมวันที่ / หัวข้อ / รายละเอียด | (อัตโนมัติทุกครั้ง) |
| 14 | **พิจารณาความเชื่อมโยงระหว่างระบบ** — ก่อนแก้/เพิ่มระบบใด ต้องไล่ดูก่อนว่าส่งผลกระทบไปยังระบบอื่นใดบ้าง แล้วตามไปแก้ให้สอดคล้องด้วย | "เปลี่ยนสูตรคำนวณพลังงาน" |
| 15 | **รายงานหลังอัปเดต + บอกสิ่งที่ต้องทดสอบ** — ต้องบอกว่า "อัปเดตอะไรไปบ้าง" และ "ให้เช็กฟังก์ชันอะไรบ้าง" | (อัตโนมัติทุกครั้ง) |

---

## DEV-001 · ข้อ 16 (มาตรฐานช่อง/แปลงแบบรูปภาพ)

เมื่อนำรูปมาแทนกล่องช่อง (แปลง/คอก/บ่อ/สวน ฯลฯ) ต้องทำตามนี้ทุกครั้ง:

| หัวข้อ | มาตรฐาน |
|---|---|
| **รูป** | เก็บใน `ภาพหน้าจอ/` อ้าง url ครั้งเดียวในซอร์ส แล้ว build แปลงเป็น base64 ฝังชุดเดียว |
| **Crop** | ถ้ารูปมี glow/เงาจาง → crop เหลือเฉพาะวัตถุจริง (Pillow: alpha > ~185 หา bbox แล้ว crop) |
| **Glow** | ใส่กลับด้วย CSS `filter: drop-shadow` (สีน้ำตาล) |
| **บีบไฟล์** | PNG โปร่ง = optimize / ภาพทึบ = JPEG q~82 |
| **Layout** | ช่องว่างลอยบนแถบหมวด ไม่มีกรอบกล่อง (`border/bg/box-shadow` ออกหมด) |
| **ขนาด** | `background-size:contain`, `aspect-ratio` ตรงสัดส่วนรูปที่ crop แล้ว, `align-self:center` |
| **เมนูลอย** | กดช่อง → เด้งเมนูลอยเฉียงขึ้น-ขวา 45° (NE) clamp ไม่ให้ล้นจอ มี 2 แถบ: 🔍 ดูแปลง / 🌱 ปลูก |
| **Zoom overlay** | พื้นหลังเบลอโปร่ง, z-index สูงกว่าทุก UI (100100+), ปิดได้ด้วยปุ่ม ✕ หรือกดนอกรูป |
| **Pinch-zoom** | ปิดที่ viewport: `maximum-scale=1.0, user-scalable=no` |

---

## DEV-001 · ข้อ 17 (มาตรฐาน Prompt ออกแบบภาพ)

เมื่อผู้ใช้ขอให้คิด Prompt สำหรับสร้างภาพ → เขียนเป็นภาษาอังกฤษ สไตล์เดียวกันทั้งเกม

**โครง Prompt (ส่วนเฉพาะวัตถุ):**
```
A charming <object> icon for a <use> ... <mood/details>, small green leaves or a
sunflower accent ... friendly farm feeling.
```

**Shared Style (วางท้ายทุก Prompt เสมอ):**
```
Style: cute 3D cartoon mobile farming game icon, glossy and polished, warm golden
tones, soft studio lighting, rounded chunky shapes, thick clean outlines, vibrant
saturated colors, subtle sparkle highlights, high detail, centered single object,
isolated on transparent background (PNG), game UI asset, app icon quality, similar
art style to Hay Day / Township / Family Farm. No text, no letters, no watermark,
no background scene.
```

**เคล็ดลับ:** สั่งทีละอัน + ขอ transparent PNG + ถ้าต้องการชุดให้บอก `"keep the exact same art style/lighting/gold palette as the previous icon"`

---

## DEV-001 · ข้อ 18 (Animation ด้วย CSS เสมอ)

เมื่อผู้ใช้ขอให้ทำภาพ/ไอคอน/โลโก้เคลื่อนไหว → ใช้ CSS `@keyframes` + `animation` เท่านั้น (ห้ามใช้ GIF/วิดีโอ)

| Effect | วิธีทำ |
|---|---|
| ลอยขึ้น-ลง | `translateY` |
| เรืองแสง | `drop-shadow` / `brightness` |
| เพชรเรืองแสง | `radial-gradient` + `mix-blend-mode` |
| ดาวกระพริบ | `opacity` / `scale` / `rotate` พร้อม `animation-delay` สลับจังหวะ |

- ทำให้นุ่มนวลสมจริง: `ease-in-out`, `infinite`
- element ที่ต้องเคลื่อนไหวซ้อน (แสง/ดาว) ใส่เป็น child `position:absolute` ภายในกล่อง `position:relative`

---

## สรุปด่วน — สิ่งที่ต้องทำ "อัตโนมัติ" ทุกการอัปเดต

1. อ่านไฟล์ก่อนแก้เสมอ (กฎข้อ 4)
2. ยืนยันก่อนลงมือ (กฎข้อ 11)
3. แก้เฉพาะสิ่งที่สั่ง ในส่วน Section ที่ถูกต้อง (กฎข้อ 1, 8, 9)
4. บันทึก UPDATE_HISTORY +0.01 (กฎข้อ 13)
5. แจ้งสรุปการเปลี่ยนแปลง + สิ่งที่ต้องทดสอบ (กฎข้อ 6, 15)
6. Build FarmGame-standalone.html (ฝัง base64 ทุกรูป) แล้ว SendUserFile (กฎ A, B)
