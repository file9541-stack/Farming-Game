# 📘 คู่มือการพัฒนาเกมส์ — Millionaire Farm Wars

> สรุปความรู้พื้นฐานเรื่องไฟล์ในโปรเจกต์ การนำเกมขึ้นเว็บ (deploy) คำศัพท์ และค่าใช้จ่าย
> เขียนแบบเข้าใจง่ายสำหรับผู้พัฒนา — อ่านแล้วสั่งงาน/ตัดสินใจได้ถูก

---

## 1. ทำไมมีไฟล์ HTML 2 ไฟล์?

มี 2 ไฟล์เพราะ **แยกหน้าที่กัน** (ตามกฎ A–B ใน CLAUDE.md)

| ไฟล์ | บทบาท | รูปภาพ | เปิดเดี่ยวได้ไหม | ห้ามลบไหม |
|---|---|---|---|---|
| **`Game REV.1.html`** | 🔧 **ซอร์สต้นฉบับ** ที่เราแก้โค้ดทุกครั้ง (มี DEV RULES, UPDATE_HISTORY, ระบบทั้งหมด) | อ้างจากโฟลเดอร์ `ภาพหน้าจอ/` (เบา ~0.5 MB) | ❌ ไม่ได้ (ต้องมีโฟลเดอร์รูป) | ⛔ **ห้ามลบเด็ดขาด** |
| **`FarmGame-standalone.html`** | 🎮 **ไฟล์ผลลัพธ์** ที่ build แล้ว เอาไว้โหลดไปเล่น/เทส | ฝัง base64 ในตัว (หนัก ~5.9 MB) | ✅ ได้ ดับเบิลคลิกเปิดเลย | ลบได้ (build ใหม่ได้เสมอ) |

### เปรียบเทียบให้เห็นภาพ
เหมือน **ไฟล์ Photoshop (.psd) กับรูป .jpg ที่ export ออกมา**
- `.psd` = ต้นฉบับมี layer แก้ได้ → **Game REV.1.html**
- `.jpg` = ไฟล์สำเร็จเอาไปใช้ → **FarmGame-standalone.html**

> **กฎเหล็ก:** แก้ที่ `Game REV.1.html` เสมอ แล้วค่อย build เป็น standalone ทีหลัง · **ห้ามลบ `Game REV.1.html`** เพราะคือหัวใจของทั้งโปรเจกต์ (เคยโดนลบไป 1 ครั้ง แล้วกู้กลับมาแล้ว)

---

## 2. คำศัพท์ที่ต้องรู้

| คำ | อ่านว่า | แปลว่า | เปรียบเทียบ |
|---|---|---|---|
| **Repo** (Repository) | รี-โพ | คลัง/กล่องเก็บไฟล์ทั้งหมดของโปรเจกต์บน GitHub | โฟลเดอร์โปรเจกต์ / Google Drive ของงาน |
| **Source** | ซอร์ส | ไฟล์ต้นฉบับที่เราเขียน/แก้โค้ด | ต้นฉบับ .psd |
| **Deploy** | ดี-พลอย | เอาเกม/เว็บขึ้นใช้งานจริงให้คนเข้าถึงได้ | ยกอาหารไปเสิร์ฟลูกค้า |
| **Develop** | ดี-เวลอป | พัฒนา/เขียนโค้ดในเครื่อง | ทำอาหารในครัว |

> repo ของเราชื่อ: **`file9541-stack/file5000`**

---

## 3. เป้าหมาย: เล่นบนเว็บ (ไม่ลง Play Store / App Store)

**ข่าวดี: เกมเป็นไฟล์ HTML อยู่แล้ว = เป็นเว็บอยู่แล้ว** ไม่ต้องแปลงเป็นแอป Android/iOS

| แบบเดิม (คิดจะลง Store) | แบบใหม่ (เล่นบนเว็บ) |
|---|---|
| ต้องห่อด้วย Cordova/Capacitor | ❌ ไม่ต้อง |
| ต้อง build APK / IPA | ❌ ไม่ต้อง |
| เสียค่าสมัคร dev account | ❌ ไม่ต้อง |
| รออนุมัติจากสโตร์ | ❌ ไม่ต้อง |
| **แค่เปิดลิงก์ก็เล่นได้เลย** | ✅ |

### วิธีเอาขึ้นเว็บ — ใช้ GitHub Pages (Source อยู่ใน repo นี้แหละ ไม่ต้องย้าย)
```
repo GitHub (Source อยู่ที่นี่)  ──เปิด Pages──>  ได้ลิงก์เว็บ เล่นได้เลย
   แก้ตรงนี้ → push                              เว็บอัปเดตเองอัตโนมัติ
```
- ได้ลิงก์แบบ `https://file9541-stack.github.io/file5000/`
- แก้เกม → push → **เว็บอัปเดตเองอัตโนมัติ**

### Deploy ได้ 2 แบบ
- **แบบ A:** `Game REV.1.html` + โฟลเดอร์ `ภาพหน้าจอ/` → เบา เบราว์เซอร์ cache รูปได้ โหลดเร็วตอนเข้าซ้ำ ✅ **แนะนำสำหรับเว็บ**
- **แบบ B:** `FarmGame-standalone.html` → ไฟล์เดียวจบ แต่หนัก 5.9 MB โหลดครั้งแรกช้านิดนึง

---

## 4. ขนาดไฟล์ — จุได้สูงสุดเท่าไหร่

| ระดับ | ขีดจำกัด | หมายเหตุ |
|---|---|---|
| **ไฟล์เดียว** | **100 MB** | เกินนี้ push ไม่ได้ (ต้องใช้ Git LFS) |
| ไฟล์เตือน | 50 MB | เกิน 50 มีคำเตือน แต่ยัง push ได้ |
| **ทั้ง repo** | แนะนำ < **1 GB** | ใช้ได้ถึง ~5 GB แต่ GitHub ขอให้ต่ำกว่า 1 GB |
| อัปโหลดผ่านหน้าเว็บ GitHub | 25 MB/ไฟล์ | |
| **GitHub Pages — เว็บที่ publish** | 1 GB | |
| **GitHub Pages — bandwidth** | 100 GB/เดือน (~17,000 ครั้งเล่น) | |

> **เกมเราตอนนี้ใช้ ~10 MB เท่านั้น = ใช้ไปแค่ ~1% ของ 1 GB** เบามาก ยังเพิ่มได้อีกเยอะ

---

## 5. ค่าใช้จ่าย — ฟรีไหม?

> **สรุป: เกมขนาดนี้ฟรี 100% ไม่ต้องจ่ายอะไรเลย**

- repo ธรรมดา **ไม่คิดเงินตามขนาด** — 1 GB เป็นแค่ "ขีดแนะนำ (soft limit)" ถ้าเกิน GitHub แค่ส่งอีเมลขอความร่วมมือ **ไม่ตัดเงินอัตโนมัติ**
- บริการที่คิดเงินแยก (เรา **ไม่ได้ใช้** ทั้งหมด):

| บริการ | ฟรีถึง | เราใช้ไหม |
|---|---|---|
| Git LFS (ไฟล์ใหญ่พิเศษ) | 1 GB | ❌ |
| GitHub Actions (รันสคริปต์อัตโนมัติ) | 2,000 นาที/เดือน | ❌ |
| GitHub Packages | 500 MB | ❌ |

### ต้องสมัคร GitHub Pro ($4/เดือน) ไหม?
> **ไม่ต้อง** — บัญชีธรรมดาก็ deploy เว็บฟรีได้

| สถานการณ์ | ต้อง Pro ไหม |
|---|---|
| เกมขนาดปกติ + repo **Public** | ❌ ฟรี |
| repo เป็น **Private** แล้วอยากเปิด Pages | ⚠️ ต้อง Pro |

> **เงื่อนไขฟรีข้อเดียว: ตั้ง repo เป็น Public (สาธารณะ)** — เกมที่จะให้คนเล่นอยู่แล้วก็เป็น Public โดยธรรมชาติ เลยฟรีสบายๆ

---

## 6. สรุปการตัดสินใจ (Quick Reference)

- ⛔ **ห้ามลบ** `Game REV.1.html` (ซอร์สต้นฉบับ) · ลบ `FarmGame-standalone.html` ได้ (build ใหม่ได้)
- 🌐 **ทิศทาง: เล่นบนเว็บ** ผ่าน GitHub Pages (ไม่ลง Store)
- 💚 **ฟรี 100%** สำหรับขนาดปัจจุบัน · ไม่ต้องสมัคร Pro · แค่ตั้ง repo เป็น Public
- 📦 ขนาดยังเหลือเฟือ (ใช้ ~10 MB จากเพดาน ~1 GB)
- 🚀 ขั้นต่อไปที่รอทำ: **deploy ขึ้น GitHub Pages** (แนะนำ deploy แบบ A)

---

## 7. บันทึกงาน — การ์ดคอกสัตว์เล็ก `.pcard` (ต้นแบบ) + เทคนิค CSS ที่ใช้

> สรุปงานปรับ **การ์ดคอกสัตว์เล็ก** (ดีไซน์ใหม่ `.pcard` ในหมวดคอกสัตว์เล็ก) ตั้งแต่ v1.01 ลงมา
> ใช้เป็น "ตำราเทคนิค" อ้างอิงเวลาทำการ์ดหมวดอื่นต่อ (คอกใหญ่/บ่อ/พืช/ไม้)

### 7.1 ที่มา
- การ์ด `.pcard` = การ์ดคอกสัตว์เล็กแบบ 5 คอลัมน์: **[เลข+ชื่อ+ดาว+รูป] · รองรับ · ผลิตได้ · ข้อมูล · ปุ่มเพิ่ม**
- เรนเดอร์ด้วย `penCardV2Html('small')` ใน `renderAnimals` · **เทสเฉพาะคอกเล็กก่อน** หมวดอื่นยังใช้ `.bcard` เดิม
- รูปที่ใช้ (process ตาม DEV-001 ข้อ 21 แล้วฝัง base64 ใน `ภาพหน้าจอ/`): `pen-small-coop.png` (รูปคอกไก่จริง) · `ui-star-on/off.png` (ดาว) · `ui-btn-plus.png` (ปุ่มบวก)

### 7.2 ประวัติการปรับ (v1.01 → v1.06)
| เวอร์ชัน | ปรับอะไร |
|---|---|
| **v1.01** | เอา panel พื้นหลังจางๆ ของ `.farm-section` (หมวดคอกเล็ก) ออก — การ์ดวางบนพื้นหลังตรงๆ (คงความกว้าง/padding เดิม) |
| **v1.02** | ย้ายหมายเลข badge เข้ามาในกล่อง (inline ซ้ายของชื่อ) + จัดรูปคอกให้อยู่กึ่งกลาง |
| **v1.03** | เยื้องแถวดาวให้เริ่มใต้ชื่อ (พ้นวงกลมเลข) + ขยับดาวขึ้นใกล้ชื่อ |
| **v1.04** | ขยายรูปคอกให้ใหญ่ขึ้น อยู่กึ่งกลางซ้าย-ขวา ไม่ชนขอบ |
| **v1.05** | ย้ายเลข badge ไปมุมบนซ้าย (เว้นระยะ ไม่ชิด) + ขยับชื่อ+ดาวขึ้น |
| **v1.06** | ขยายรูปคอกอีกนิด (108%) + เยื้องไปทางซ้าย |

### 7.3 เทคนิค CSS ที่ใช้ (นำไปใช้ซ้ำได้)
- **กรอบสีไล่เฉด + มุมโค้ง** (border-image โค้งมุมไม่ได้ → ใช้ double-background):
  ```css
  background:linear-gradient(...เนื้อครีม...) padding-box,
             linear-gradient(145deg,#a9763a,#7a4d1c,#96632a) border-box;
  border:3px solid transparent; border-radius:18px;
  ```
  (กรอบใหญ่ครีมอ่อน · กรอบเล็ก/ปุ่มโทนเข้มกว่า · เคยลองทองแต่จางไป เลยใช้น้ำตาลเข้ม)
- **เส้นแบ่งคอลัมน์แนวตั้งจางหัว-ท้าย:** `border-left` + `border-image:linear-gradient(to bottom, transparent, สี, transparent)`
- **แถบลอย hover "รอการพัฒนา"** (CSS ล้วน ไม่ใช้ JS): `.pc-row-soon:hover .pc-tip{ opacity:1 }` (ดูแถว "อื่นๆ")
- **หมายเลข badge มุมการ์ด:** `position:absolute; top/left` (เว้นระยะ ไม่ยื่นออกมุม) · กดสลับลำดับหมวดผ่าน delegation `.pc-numbadge[data-secidx]` → `openSectionMoveModal`
- **จัด/ขยายรูปในช่อง:** คุมด้วย `.pc-thumb-img` (`width/height` %, `background-position`, `margin-left` เยื้อง) + `background-size:contain`
- **ชื่อยาวกันตกบรรทัด:** `white-space:nowrap` + `flex:0 1 auto` (ปล่อยแท็ก NEW ล้นไปทางขวา)

### 7.4 การนำขึ้น main (deploy)
- Branch พัฒนา: `claude/session-setup-4hfi22` → build `FarmGame-standalone.html` + ก๊อปทับ `index.html` ทุกครั้ง
- Merge `origin/main` เข้ามาก่อน (เก็บไฟล์โฟลเดอร์ `001` ต้นฉบับรูป) แล้ว `git push origin HEAD:main`
- GitHub Pages build เว็บ live เองอัตโนมัติ: **https://file9541-stack.github.io/Farming-Game/**
