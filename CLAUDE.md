# CLAUDE.md

## กฎการทำงานกับโปรเจกต์เกมนี้ (ผู้ใช้กำหนด)

1. **ฝังรูปเป็น base64 เสมอ** — เมื่อผู้ใช้ให้นำรูปที่เพิ่มเข้ามาไปใช้ในเกม
   ให้ฝังรูปนั้นเข้าไปในไฟล์ HTML โดยตรง (data URI base64) ไม่ใช้การอ้างอิง
   ไฟล์ในโฟลเดอร์แยก เพื่อให้ได้ไฟล์เดียวจบ โหลดไปเปิดได้ทันที ไม่ต้องมีโฟลเดอร์
   ไม่ต้องใช้ raw.githack

2. **อัปเดตเสร็จต้องส่งไฟล์เดียวจบให้ทันที** — ทุกครั้งที่อัปเดตเกมเสร็จ ให้รวมเป็น
   ไฟล์ HTML เดียวที่เปิดได้ทันที (รูป/ของอื่น ๆ ฝังในตัว) แล้วส่งไฟล์ให้ผู้ใช้
   โดยตรงผ่าน SendUserFile เพื่อให้ผู้ใช้โหลดไปเทสได้เลยโดยไม่ต้องเสียเวลา

3. **มาตรฐานการตั้งค่า "ช่อง/แปลงแบบรูปภาพ + การโต้ตอบ"** — เมื่อผู้ใช้ให้นำรูปมาแทน
   กล่องช่อง (แปลงปลูกพืช/คอกสัตว์/บ่อปลา ฯลฯ) ให้ทำตามขั้นตอนนี้ทุกครั้ง:

   3.1 **เตรียมรูป (crop + บีบ + ฝัง)**
   - เก็บรูปต้นฉบับไว้ในโฟลเดอร์ `ภาพหน้าจอ/` แล้วอ้างอิงด้วย `url('ภาพหน้าจอ/ชื่อไฟล์')`
     "ครั้งเดียว" ในซอร์ส แล้วใช้สคริปต์ build แปลงเป็น data URI base64 ตอนสร้าง
     ไฟล์ standalone (กฎข้อ 1) — ฝังภาพชุดเดียว ไม่ฝังซ้ำให้ไฟล์บวม
   - ถ้ารูปมี glow/เงาจาง ๆ รอบนอก ให้ **crop เหลือเฉพาะตัววัตถุจริง** (ใช้ Pillow
     threshold ที่ alpha > ~185 หา bbox แล้ว crop) เพื่อตัดขอบ glow ที่ทำให้เกิด
     "เส้นจาง ๆ" ตอนขยาย — แล้วใส่ glow กลับด้วย CSS `drop-shadow` แทน (สีน้ำตาล)
   - ย่อขนาด + บีบ (PNG โปร่งใช้ optimize, ภาพทึบใช้ JPEG q~82) ให้เล็กที่สุด

   3.2 **การแสดงช่องว่าง (ลอยบนแถบหมวด ไม่มีกรอบกล่อง)**
   - เอา border/background/box-shadow ของกล่องออก, ใช้ `background-size:contain`,
     ตั้ง `aspect-ratio` ให้ตรงสัดส่วนรูปที่ crop แล้ว, `align-self:center` ให้อยู่
     กลางช่อง, ใส่ glow ขอบด้วย `filter:drop-shadow(... สีน้ำตาล ...)`
   - ป้ายชื่อช่องวางทับกลางรูป (absolute center) ตัวเล็ก มีเงาตัวอักษรให้อ่านชัด

   3.3 **การโต้ตอบเมื่อกดช่อง (เมนูลอย + ภาพขยาย)**
   - กดช่องว่าง → เด้ง "เมนูลอย" ตำแหน่งเฉียงขึ้น-ขวา 45° (ตะวันออกเฉียงเหนือ)
     ของช่องนั้น (clamp ไม่ให้ล้นจอ), ขนาดแถบเท่าปุ่มหลัก, มี 2 แถบเรียงบน-ล่าง:
     · แถบบน "🔍 ดูแปลงใกล้ๆ" → เปิดภาพขยาย (overlay)
     · แถบล่าง "🌱 ปลูกพืช / รื้อถอน" → เปิดหน้าจัดการเดิม
   - ปิดเมนูเมื่อกดนอกเมนู หรือเลื่อนกล่องเนื้อหา
   - ภาพขยาย (zoom overlay): พื้นหลังเบลอโปร่ง, **z-index สูงกว่าทุก UI** (เช่น
     100100 เหนือแชท/ล็อกอิน) กัน UI อื่นลอยทับเป็นเส้น, มีปุ่มปิด ✕, กดนอกภาพปิดได้,
     แสดงเฉพาะรูปวัตถุที่ crop แล้ว + ชื่อกำกับใต้ภาพ, ขนาดพอเหมาะ (ไม่เต็มจอเกินไป)

   3.4 **อื่น ๆ** — ปิด pinch-zoom ของทั้งเกมที่ viewport
   (`maximum-scale=1.0, user-scalable=no`)

